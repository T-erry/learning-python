# set = unordered collection and unindexed.
        # No duplicate value


utensils ={"fork", "spoon", "knife"}
dishes ={"bowl", "cup", "plate", "knife"}

#Adds a given element to a set
# utensils.add("plate")

#Removes all elements from the set
#utensils.clear()

 # Removes the element from the set
# utensils.remove("fork")

#Adds elements to the set
# utensils.update(dishes)

#Returns a set that has the union of all sets
#dinner_table = utensils.union(dishes)

#Returns a set that is the difference between two sets
# what exist in dishes that utensils don't have
# print(dishes.difference(utensils))

#Returns a set that has the intersection
# elements in sets that are common
# print(utensils.intersection(dishes))

# for i in dinner_table:
#     print(i)